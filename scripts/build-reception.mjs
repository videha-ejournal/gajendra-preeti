import fs from 'node:fs';
import assert from 'node:assert/strict';

export function buildReception() {
 const data=JSON.parse(fs.readFileSync('content/reception-gists.json','utf8'));
 const {books,articles}=data;
 const base='/gajendra-preeti', origin='https://videha-ejournal.github.io';
 const dir='public/criticism/reception';
 const manifest=JSON.parse(fs.readFileSync('public/criticism/manifest.json','utf8'));
 const e=x=>String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const route=b=>`${base}/criticism/reception/${b.id}.html`;
 const href=a=>`${base}/criticism/reception/${a.book}.html#${a.id}`;
 const main=articles.filter(a=>a.kind==='contribution');
 assert.equal(new Set(articles.map(a=>a.book+':'+a.id)).size,articles.length);
 for(const b of books){
  const rows=articles.filter(a=>a.book===b.id);
  assert.equal(rows.filter(a=>a.kind==='contribution').length,b.expectedContributions,`Contents coverage: ${b.id}`);
  for(const a of rows){assert(a.author&&a.title&&a.gist);assert(a.start>=1&&a.end>=a.start&&a.end<=b.pages);}
 }
 const disclosure='These AI-assisted gists summarize the named contributors’ arguments in the supplied English PDFs. The original authors did not write these summaries; no named human editorial review is recorded. Attribution does not certify every historical claim, priority claim or reported incident. Translation fidelity against the Maithili originals has not been independently checked.';
 const citation=(a,b)=>`${a.author}. “${a.title}.” In ${b.title}, edited by ${b.editor}${b.translator?`, translated from Maithili into English by ${b.translator}`:''}, pp. ${a.start}–${a.end}. Supplied English PDF: ${b.file}; publication year not established.`;
 function shell(title,description,url,body){
  const schema={'@context':'https://schema.org','@type':'CollectionPage',name:title,description,url:origin+url,inLanguage:'en',isPartOf:{'@type':'WebSite',name:'The Videha Literary Atlas',url:origin+base+'/'}};
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${e(title)} | Videha Reading Room</title><meta name="description" content="${e(description)}"><link rel="canonical" href="${origin}${url}"><meta name="theme-color" content="#141c26"><meta property="og:title" content="${e(title)}"><meta property="og:description" content="${e(description)}"><meta property="og:type" content="website"><meta property="og:url" content="${origin}${url}"><meta property="og:image" content="${origin}${base}/images/gajendra-thakur.jpg"><meta name="twitter:card" content="summary"><meta name="twitter:title" content="${e(title)}"><meta name="twitter:description" content="${e(description)}"><meta name="twitter:image" content="${origin}${base}/images/gajendra-thakur.jpg"><link rel="icon" href="${base}/icon.svg"><link rel="stylesheet" href="${base}/criticism/style.css"><link rel="stylesheet" href="${base}/criticism/reception/reception.css"><link rel="stylesheet" href="${base}/reading-tools.css"><script defer src="${base}/reading-tools.js"></script><script defer src="${base}/criticism/reception/reception.js"></script><script type="application/ld+json">${JSON.stringify(schema).replaceAll('<','\\u003c')}</script></head><body><a class="skip" href="#main">Skip to main content</a><header class="mast"><a class="brand" href="${base}/en/">PREETI <span>×</span> GAJENDRA<small>THE VIDEHA LITERARY ATLAS</small></a><nav aria-label="Main navigation"><a href="${base}/en/">English home</a><a href="${base}/" lang="mai">मैथिली</a><a href="${base}/criticism/">Reading room</a></nav></header><main id="main"><nav class="breadcrumb" aria-label="Breadcrumb"><a href="${base}/criticism/">Criticism</a><span aria-hidden="true">/</span><a href="${base}/criticism/reception/">Contributors’ perspectives</a></nav>${body}</main><div id="videha-reading-tools"></div><footer><p>More than one way to read a life’s work.</p><div><a href="${base}/criticism/">All editorial readings</a><a href="${base}/criticism/reception/#sources">Source editions & method</a><a href="#main">Back to top ↑</a></div><small>Prepared from the supplied English editions · 9 September 2026.<br>${e(disclosure)}</small></footer></body></html>`;
 }
 const kinds={contribution:'Signed contribution',editorial:'Editorial framing',supplement:'Supporting material'};
 const filters=`<form class="gist-filters" role="search"><label>Search title, contributor or gist<input id="gist-query" type="search" placeholder="Try Panji, children, Kalpana…" autocomplete="off"></label><label>Focus<select id="gist-focus"><option value="all">All subjects</option><option value="preeti">Preeti Thakur</option><option value="gajendra">Gajendra / Videha</option><option value="context">Editorial & contextual material</option></select></label><button type="reset">Reset</button></form><p class="gist-count" role="status" aria-live="polite">All entries shown</p><noscript><p>Every gist is visible below. Use your browser’s Find command to search without JavaScript.</p></noscript>`;
 const booksCards=books.map((b,i)=>`<a class="volume-card" href="${route(b)}"><span class="eyebrow">VOLUME ${i+1} / ${b.expectedContributions} CONTRIBUTIONS</span><h2>${e(b.title)}</h2><p>${e(b.description)}</p><span>Read every article’s gist →</span></a>`).join('');
 const edition=b=>`<article class="edition" id="source-${b.id}"><h3>${e(b.title)}</h3><p>Edited by <strong>${e(b.editor)}</strong>. ${b.translator?`English translation: <strong>${e(b.translator)}</strong>. `:''}${b.pages} PDF pages. ${e(b.editionNote)}</p><p>Consulted file: <cite>${e(b.file)}</cite>. Supplied by the site owner; citations refer to this English edition, not a verified online facsimile. Page references count PDF pages, including front matter; a shared boundary page may contain the end of one contribution and the start of the next.</p><details><summary>Edition identification and coverage</summary><p>SHA-256: <code>${b.sha256}</code></p><p>${b.bytes.toLocaleString('en-US')} bytes. Title pages and article boundaries were checked in the supplied PDFs. Main contributions are counted separately from editorials and appendices. ${b.id==='preeti-karan'?'Front matter and section dividers occupy pp. 1–9, 38, 80 and 130. Pages 500–501 and 528–531 contain no extracted article text.':'Front matter and section dividers occupy pp. 1–10, 55 and 71.'} Repeated contributors have separate entries for their separate contributions.</p></details></article>`;
 const indexBody=`<section class="reception-hero"><p class="eyebrow">CONTRIBUTORS’ PERSPECTIVES / ENGLISH BOOK GISTS</p><h1>Many voices.<br><em>Two volumes.</em></h1><p class="intro">Criticism, memories, interviews and editorial debate—each returned to its author and its place in the book.</p><div class="reception-stats"><span><strong>${main.length}</strong>main contributions</span><span><strong>${articles.filter(a=>a.kind==='editorial').length}</strong>editorial introductions</span><span><strong>${articles.filter(a=>a.kind==='supplement').length}</strong>supporting summaries</span></div><p class="scope">Every main contribution in both supplied English volumes is summarized below. These contributor perspectives sit alongside the atlas’s existing editorial readings.</p></section><section class="volume-grid" aria-label="The two source books">${booksCards}</section><section class="gist-library"><h2>Find a contributor or a subject</h2>${filters}<div class="gist-index">${articles.map(a=>`<article data-gist data-focus="${a.group}" data-search="${e([a.title,a.author,a.gist,books.find(b=>b.id===a.book).title].join(' ').toLowerCase())}"><p class="eyebrow">${e(books.find(b=>b.id===a.book).title)} · ${kinds[a.kind]}</p><h3><a href="${href(a)}">${e(a.title)}</a></h3><p class="contributor">${e(a.author)} · pp. ${a.start}–${a.end}</p><p>${e(a.gist)}</p><a class="citation-link" href="${href(a)}">Citation & context →</a></article>`).join('')}</div></section><section id="sources" class="source-method"><p class="eyebrow">SOURCE EDITIONS / CONSULTED 9 SEPTEMBER 2026</p><h2>Read the attribution as closely as the gist.</h2><p>${e(disclosure)}</p><p>Some essays are appreciative profiles, some detailed literary criticism, and others explicit disagreements. Editorial responses are identified as such. English headings are retained or lightly shortened for navigation; spelling variants and notable version differences are recorded beside the relevant gist. No publication year or translator credit has been invented. Article IDs belong to these volumes and are separate from the atlas’s P/G catalogue identifiers.</p>${books.map(edition).join('')}</section>`;
 fs.mkdirSync(dir,{recursive:true});
 fs.writeFileSync(dir+'/index.html',shell('Contributors’ perspectives — two books, every article',`${main.length} attributed gists from Preeti Karan Setu Banhal and Setusham, with book, author, editor and page references.`,base+'/criticism/reception/',indexBody));
 for(const b of books){
  const rows=articles.filter(a=>a.book===b.id);
  const body=`<section class="reception-hero volume-hero"><p class="eyebrow">${b.expectedContributions} MAIN CONTRIBUTIONS / SUPPLIED ENGLISH EDITION</p><h1>${e(b.title)}</h1><p class="intro">${e(b.subtitle)}</p><p>Edited by <strong>${e(b.editor)}</strong>${b.translator?` · English translation by <strong>${e(b.translator)}</strong>`:''}</p><p class="scope">Gists of the contributors’ writing, with full references. Each summary reports the argument of its source; it does not replace the article or certify all of its claims.</p><p><a href="#edition">Edition details ↓</a> · <a href="${base}/criticism/reception/">Search both books →</a></p></section><nav class="volume-toc" aria-label="Contents of this volume"><h2>In the book’s order</h2><ol>${rows.map(a=>`<li><a href="#${a.id}">${e(a.title)}</a><small>${e(a.author)} · ${a.start}–${a.end}</small></li>`).join('')}</ol></nav><section class="full-gists" aria-label="Article gists">${rows.map(a=>`<article class="full-gist" id="${a.id}"><div class="gist-margin"><span class="eyebrow">${kinds[a.kind]}</span><p>pp. ${a.start}–${a.end}</p><a href="#${a.id}" aria-label="Permanent link to ${e(a.title)}">Link to this gist ↗</a></div><div><h2>${e(a.title)}</h2><p class="contributor">${e(a.author)}</p><p class="gist-prose">${e(a.gist)}</p>${a.note?`<p class="version-note"><strong>Source note.</strong> ${e(a.note)}</p>`:''}<div class="gist-citation"><h3>Book reference</h3><p>${e(citation(a,b))}</p><p class="summary-credit">Summary prepared for the Videha Literary Atlas with AI assistance, 9 September 2026. It is not a text signed by the article’s author.</p></div>${a.related.length?`<p class="read-alongside">Related atlas criticism: ${a.related.map(id=>`<a href="${base}/criticism/${id}.html">${e(manifest.find(r=>r.id===id).titleEn)}</a>`).join(' · ')}. These may discuss a different edition or a related project.</p>`:''}</div></article>`).join('')}</section><section id="edition" class="source-method"><h2>The edition behind these references</h2>${edition(b)}<p>${e(disclosure)}</p><p><a href="${route(books.find(x=>x.id!==b.id))}">Continue to ${e(books.find(x=>x.id!==b.id).title)} →</a></p></section>`;
  fs.writeFileSync(dir+'/'+b.id+'.html',shell(b.title+' — article-by-article gists',`${b.expectedContributions} contributions summarized with their authors and page references. Edited by ${b.editor}.`,route(b),body));
 }
 console.log(`Built reception library: ${main.length} contributions and ${articles.length-main.length} editorial/supporting gists.`);
 return [origin+base+'/criticism/reception/',...books.map(b=>origin+route(b))];
}
